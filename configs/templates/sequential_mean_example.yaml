command: sequential-mean
input: examples/example_user_level.csv
out: out/yaml_sequential_mean
audit: true

params:
  group_col: group
  control: control
  test: test
  y: revenue
  mode: group_sequential
  alpha: 0.05
  spending: obrien_fleming
  effect_direction: two_sided

  looks: "4,8,10"
  min_n_per_group: 2
